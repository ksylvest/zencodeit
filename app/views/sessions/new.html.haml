%p.center
  = link_to image_tag(asset_path('social/twitter.png' ), alt: "Sign in with Twitter" ), auth_path(:twitter )
  = link_to image_tag(asset_path('social/facebook.png'), alt: "Sign in with Facebook"), auth_path(:facebook)
